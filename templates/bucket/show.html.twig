{% extends 'frontend.html.twig' %}

{% block title %}Bucket {{ bucket.locus.excavation.site }}{{ bucket.locus.excavation.season }}-{{ bucket.locus.excavation.trench }}/{{ bucket.locus.number }}/{{ bucket.number }} | Berenike.project{% endblock %}

{% block top %}
  <span title="site">PB {{ bucket.number }}</span>
{% endblock %}

{% block body %}
  {% include 'bucket/_bucket.html.twig' with {'bucket': bucket} %}

  <div class="actions" style="margin-top: 1em;">
    <a href="{{ path('PapyrillioBerenike_BucketEdit', {'id': bucket.id}) }}" 
       class="ui-button ui-widget ui-state-default ui-corner-all ui-button-text-only">
      <span class="ui-button-text">Edit</span>
    </a>
    <a href="{{ path('PapyrillioBerenike_BucketList') }}" 
       class="ui-button ui-widget ui-state-default ui-corner-all ui-button-text-only">
      <span class="ui-button-text">Back to Buckets</span>
    </a>
    {% if app.user %}
    <form method="post" action="{{ path('PapyrillioBerenike_AdminDeleteBucket', {'id': bucket.id}) }}" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this bucket?');">
      <button type="submit" class="ui-button ui-widget ui-state-default ui-corner-all ui-button-text-only" style="color: #cc0000;">
        <span class="ui-button-text">Delete</span>
      </button>
    </form>
    {% endif %}
  </div>
{% endblock %}
