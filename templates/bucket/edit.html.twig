{% extends 'frontend.html.twig' %}

{% block title %}Edit Bucket{% endblock %}

{% block top %}Edit Bucket{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/locus-autocomplete.css') }}">
    <style>
        .ui-widget label {
            font-weight: bold;
            display: inline-block;
            width: 12em;
        }
    </style>
{% endblock %}

{% block body %}
    {{ form_start(form, {'attr': {'class': 'ui-widget'}}) }}
    <div class="ui-widget ui-widget-content ui-corner-all" style="padding: 1em; margin-bottom: 1em;">
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1em;">
            <div style="display: flex; flex-direction: column; gap: 1em;">
                {{ form_row(form.number, {'attr': {'class': 'ui-widget ui-widget-content ui-corner-all'}}) }}
                {{ form_row(form.locusSearch) }}
                {{ form_row(form.locus, {'attr': {'class': 'ui-widget ui-widget-content ui-corner-all'}}) }}
                {{ form_row(form.dating, {'attr': {'class': 'ui-widget ui-widget-content ui-corner-all'}}) }}
            </div>
            <div>
                {{ form_row(form.remarks, {'attr': {'class': 'ui-widget ui-widget-content ui-corner-all', 'rows': 10}}) }}
            </div>
        </div>
    </div>

    <div style="margin: 1em 0;">
        <button type="submit" class="ui-button ui-widget ui-state-default ui-corner-all ui-button-text-only">
            <span class="ui-button-text">Save</span>
        </button>
        <a href="{{ path('PapyrillioBerenike_BucketShow', {'id': bucket.id}) }}" class="ui-button ui-widget ui-state-default ui-corner-all ui-button-text-only">
            <span class="ui-button-text">Cancel</span>
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/locus-autocomplete.js') }}"></script>
{% endblock %}
